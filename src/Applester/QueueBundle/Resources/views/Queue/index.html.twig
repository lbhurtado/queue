{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Queue list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Mobile</th>
                <th>Handle</th>
                <th>Date</th>
                <th>Remarks</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('queue_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.mobile }}</td>
                <td>{{ entity.handle }}</td>
                <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.remarks }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('queue_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('queue_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('queue_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
